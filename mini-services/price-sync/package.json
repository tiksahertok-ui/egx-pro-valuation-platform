{
  "name": "egx-price-sync",
  "version": "1.0.0",
  "description": "EGX Pro - Real-Time Price Sync Worker. Fetches EGX stock prices from Yahoo Finance and Supabase, falls back to hardcoded data, and updates the database on 15-minute intervals during trading hours.",
  "private": true,
  "scripts": {
    "sync": "bun run sync.ts --once",
    "daemon": "bun --hot sync.ts",
    "dev": "bun --hot sync.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.5",
    "typescript": "^5.0.0"
  }
}
